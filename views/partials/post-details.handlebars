<div>
    <div class="d-flex justify-content-between mb-1 title-bar px-2 align-items-center">
        <h2 class="fs-4">{{title}}</h2>
        <p class="fs-6 fst-italic fw-lighter">Posted by {{user.username}} on {{date_created}}
    </div>
    <p class="fs-5 px-4">{{content}}</p>
</div>